Use the `specimpact-onboard` Skill for the current workspace. Start from the project MCP Resource,
keep all paths inside the workspace, and stop before any external transmission until elicitation is
accepted.
