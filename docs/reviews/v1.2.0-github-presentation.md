# v1.2.0 GitHub Presentation Review

## Implementation summary

The repository landing page now explains the problem, workflow, current GUI, architecture,
privacy boundary, supported inputs, limitations, and five-minute onboarding path without mixing in
obsolete pre-v1.2 screenshots or unfinished-phase messaging.

## Changed files summary

- Rebuilt the Japanese README around the current v1.2 product and review workflow.
- Added two GitHub-rendered Mermaid diagrams for onboarding/change management and architecture.
- Replaced legacy GUI captures with current Dashboard and Knowledge Graph screenshots.
- Added concise version, runtime, and license badges plus a visible quality-gate section.
- Updated the GitHub About description and added Dirty Excel/change-impact topics.
- Corrected the GUI manual's obsolete source-freshness note.

## Verification

- `npm run check`: pass
- `npm run build`: pass; committed frontend bundle remains reproducible
- `pytest -q`: 145 passed
- `ruff check .`: pass
- `python -m compileall -q specimpact`: pass
- `specimpact release-check ./examples/evaluation/release_cases.yml`: pass, 21 cases
- README image paths and local documentation links resolve in the repository.

## Known limitations

- The repository does not host a separate documentation website.
- CI publishing remains separate because the current GitHub OAuth credential lacks workflow scope.
- GitHub social preview artwork is not configured; the README uses product screenshots instead.

## Scope check

No runtime behavior, graph semantics, LLM policy, report schema, or private invention material was
changed.
