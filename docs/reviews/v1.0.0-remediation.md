# Review: v1.0.0 External Review Remediation

## Implementation Summary

Replaced the Aurora-specific rule table with a convention-based Markdown parser, integrated all
loaders into the common graph store, added source replacement merge semantics, alias ambiguity
handling, multi-entity analysis, rejected-relation filtering, formal schemas, and OSS metadata.
The second review remediation added root-manifest pruning, root-relative stable IDs, normalized
relation evidence supports, packaged runtime schema validation, wheel-safe publication gates, and
normalized oracle-content release checks.
The third review remediation added fail-fast structured/tabular source collision handling, strict
alias entry types, scoped OpenAPI evidence lines, table-scoped DDL evidence lines, and a single
schema source copied into wheels at build time.
The fourth review remediation rejects cross-type alias collisions, stages Markdown aliases and
source manifests until graph merge succeeds, mirrors schema build output, checks the source security
policy, parses privacy backend YAML exactly, and applies CLI input-error handling consistently.
The fifth review remediation validates nested OpenAPI mappings and converts corrupt Excel workbook
errors into non-traceback CLI input errors.

## Verification Scope

- Unknown Payment domain ingest and analyze
- Multi-entity change detection
- Confirmed, unconfirmed, and rejected relation behavior
- Alias collision and immediate approved-alias resolution
- Section, chunk, and evidence reference integrity
- Markdown, OpenAPI, DDL, CSV, and Excel graph merge behavior
- Input errors and config preservation
- Removed-document pruning and duplicate-relation evidence integrity
- Structured evidence source lines and invalid structured-input handling
- Installed-wheel release-check without traceback
- Golden, Evaluation, and Holdout release gates with normalized oracle-content hashing
- Same-name structured and tabular source collision rejection
- Alias entry mapping, list, string, and canonical-type validation
- Repeated OpenAPI field evidence scoped to request and response nodes
- Root schema and wheel-packaged schema byte equality
- Cross-type alias collision rejection
- Failed Markdown ingest leaves alias, manifest, and JSONL state byte-identical
- Incremental schema build removes deleted output schemas
- Source release gate verifies the security policy contact
- Privacy doctor exact backend parsing and non-traceback CLI input errors
- Malformed OpenAPI and corrupt Excel workbook input errors

## Publication Metadata

Repository metadata and maintainer security contact are configured in `pyproject.toml`,
`SECURITY.md`, and `specimpact/resources/publication.json`.

## Verification Results

- `pytest`: 118 passed
- `ruff check .`: passed
- Quickstart: initialized state, ingested 10 documents, analyzed the credit-limit change, rendered
  JSON, and resolved the Japanese API display name
- Source-tree `release-check`: all quality checks passed, including publication metadata checks
- Installed-wheel `release-check`: completed without traceback and loaded packaged schemas
