# Review: v0.5.0

## Implementation Summary

Added optional Neo4j target configuration, Obsidian Markdown export, review result import,
relation baseline creation, and graph diff.

## Changed Files

Added integration module, sample review decisions, integration docs, tests, CLI commands, and
README usage.

## Manual Verification Commands

```powershell
python -m specimpact backend set neo4j --uri bolt://localhost:7687
python -m specimpact export-obsidian ./.specimpact/vault
python -m specimpact review import ./examples/credit_card_enrollment/reviews/change_credit_limit.review.json
python -m specimpact baseline create v05-manual
python -m specimpact graph diff v05-manual
python -m specimpact backend set local
```

## Test Results

`pytest`: 23 passed.

## Lint Results

`ruff check`: passed.

## Scope Check

- [x] Local backend remains default
- [x] Neo4j configuration is optional
- [x] Obsidian export reuses Markdown report
- [x] No cloud service required
- [x] No confidence field output

## Evidence Review

- [x] Review decisions remain local
- [x] Baseline compares relation IDs

## Model Review

- [x] Internal IDs remain stable
- [x] Display names are not used as IDs

## Known Limitations

Neo4j support records an optional target configuration. Synchronization is intentionally left to
deployment integration because local JSONL remains the required default backend.

## Next Phase Readiness

Ready for v1.0.0.
