# Review: v0.1.0-alpha-1

## Implementation Summary

Implemented local Markdown/txt ingest, manual alias loading, stable internal IDs, JSONL graph
state, rule-based evidence extraction, change analysis, Markdown/JSON reports, and `why`.

## Changed Files

Added the Python package, project metadata, roadmap/specs, Aurora enrollment sample, and alpha-1
tests.

## Manual Verification Commands

```powershell
python -m specimpact init
python -m specimpact ingest ./examples/credit_card_enrollment/docs --aliases ./examples/credit_card_enrollment/aliases.yml
python -m specimpact analyze ./examples/credit_card_enrollment/changes/change_credit_limit.md
python -m specimpact report --format markdown
python -m specimpact report --format json
python -m specimpact why カード入会申込API
```

## Test Results

`pytest`: 5 passed.

## Lint Results

`ruff check`: passed.

## Scope Check

- [x] No future-phase commands implemented
- [x] No future-phase placeholder modules created
- [x] No external LLM calls unless allowed by this phase
- [x] No confidence field output

## Evidence Review

- [x] Must Review items have evidence or direct change-request evidence
- [x] Evidence includes quote
- [x] Evidence includes source location
- [x] Evidence supports entity or relation

## Model Review

- [x] Internal IDs are stable
- [x] Display names are not used as IDs
- [x] Relations contain evidence_ids
- [x] Relations contain polarity/status/match_type

## Known Limitations

Rule extraction intentionally targets the documented Aurora demo vocabulary. Structured loaders,
suggested aliases, inspection UX, and trace-backed explanations are deferred to their phases.

## Next Phase Readiness

Ready for v0.1.0-alpha-2.
