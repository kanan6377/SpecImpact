# Review: v0.2.0

## Implementation Summary

Stabilized documented JSON contracts, added relation status workflow, alias add/remove UX, and
three-case dataset evaluation.

## Changed Files

Added schema/model docs, evaluation expected files and manifest, v0.2 tests, CLI commands, and
README usage.

## Manual Verification Commands

```powershell
python -m specimpact relations list
python -m specimpact relations set-status rel.api.card_application.submit.request_field confirmed
python -m specimpact aliases add api.card_application.submit submit-v2
python -m specimpact aliases remove api.card_application.submit submit-v2
python -m specimpact eval --dataset ./examples/credit_card_enrollment/evaluation/cases.yml
```

## Test Results

`pytest`: 14 passed.

## Lint Results

`ruff check`: passed.

## Scope Check

- [x] No future-phase commands implemented
- [x] No future-phase placeholder modules created
- [x] No external LLM calls unless allowed by this phase
- [x] No confidence field output

## Evidence Review

- [x] Must Review items have evidence or direct change-request evidence
- [x] Evidence includes quote
- [x] Evidence includes source location
- [x] Evidence supports entity or relation

## Model Review

- [x] Internal IDs are stable
- [x] Display names are not used as IDs
- [x] Relations contain evidence_ids
- [x] Relations contain polarity/status/match_type

## Known Limitations

The schema contract is documented and tested through model round trips. Machine-readable export
can be added after loader stabilization if needed.

## Next Phase Readiness

Ready for v0.3.0.
